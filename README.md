# HACS2 Custom Integration

This is a custom integration for Home Assistant.

## Installation
1. Install via HACS: Add this repo as a custom repository.
2. Restart Home Assistant.
3. Configure via UI or YAML.

## Configuration
Add to your `configuration.yaml`:
```yaml
hacs2:
  example_option: value
